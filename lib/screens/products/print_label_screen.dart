import 'package:flutter/material.dart';
import 'package:pos_app/widgets/app_drawer.dart';

class ProductsPrintLabelScreen extends StatelessWidget {
  const ProductsPrintLabelScreen({Key? key}) : super(key: key);
  static const routeName = '/products-print-label';

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      drawer: AppDrawer(),

    );
  }
}
