import 'package:flutter/material.dart';
import 'package:pos_app/widgets/app_drawer.dart';

class SellAddQuotationScreen extends StatelessWidget {
  const SellAddQuotationScreen({Key? key}) : super(key: key);
  static const routeName = '/sell-add-quotation';

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      drawer: AppDrawer(),

    );
  }
}
